"use strict";

class Character{
    constructor(target,init_x,init_y){ /*Test, need to implemente character shit over here*/
        this.bitmap = new createjs.Bitmap(target);
        this.bitmap.x = init_x;
        this.bitmap.y = init_y;
    }

    Animation(){
                                 /*Depois as animacoes tem de ficar aqui!!! */
    }
}